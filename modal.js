function modal(idx){
/*
   //Download from main table.
 //   let index = idx.id;
      let index = idx;
      let x = document.getElementById(index);
            
      var row = [], cols = x.querySelectorAll(".export");
        
        for (let j = 0; j < cols.length; j++){
            if (cols[j].type == "text" || cols[j].type == "select-one") {
                row.push(cols[j].value);
            }
        }
        
        let cols2 = x.querySelectorAll(".modal2 tr");
      
       // console.log(table);
       // let cols2 = table.getElementsByTagName("tr");
       // let cols2 = table.rows.length;
        
        for (var i = 0; i < cols2.length; i++){
                let rows = cols2[i].querySelectorAll(".import");
             
                    rows.innerHTML = row[i];
                    console.log(rows);
        }
        */
};